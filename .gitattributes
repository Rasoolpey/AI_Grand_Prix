# Normalize line endings to LF in the repo; let Git handle local conversions
* text=auto eol=lf

# MATLAB files — always LF
*.m text eol=lf

# Markdown docs
*.md text eol=lf

# JSON config files
*.json text eol=lf

# Binary MATLAB data files — do not convert
*.mat binary
*.fig binary
*.mex* binary
